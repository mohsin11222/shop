name: CI Test

on:
  push:
    branches:
      - main  # ✅ make sure your branch is also named 'main'

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repo
        uses: actions/checkout@v3

      - name: Test step
        run: echo "CI is working!"
